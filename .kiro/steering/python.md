---
inclusion: auto
---

# Python Environment

- This workspace uses `python3` (not `python`). Always use `python3` and `pip3` in all shell commands.
- For running tests: `python3 -m pytest`
- For compiling/checking syntax: `python3 -m py_compile`
- For installing packages: `pip3 install`
